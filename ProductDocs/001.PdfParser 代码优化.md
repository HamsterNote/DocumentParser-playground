# 背景

现在的 PdfParser 根据 pdfjs 生成的中间态数据：IntermediateDocument，再经过 HtmlParser 产生 HTML 输出到 Web 页面上，产生的文字位置与 pdfjs 直接用 render 输出的位置不一致，主要表现是在单个 Page 中文字的位置是反的（上下颠倒，不是指文字本身颠倒，是所在位置本来应该在上面，结果出现在了下面），可能需要代码进行校正

# 需要做的

- [x] 先模拟生成一份单页的 HTML 文件（或者字符串），通过 HtmlParser 转成 中间态数据IntermediateDocument，再通过 PdfParser 转成 PDF，再转回中间态IntermediateDocument，对比区别并分析问题，修复问题

# 注意事项

目前来看，PdfParser 和 HtmlParser 都会有 bug，它们是还没有稳定、完善的，如果发现它们有问题，可以修复问题，但是基类内容（也就是 HamsterDocument）里面的东西，做改动要谨慎，尽量不要动他们的结构，除非迫不得已
